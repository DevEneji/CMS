{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "cd frontend && npm install && npm run build"
    },
    "deploy": {
      "startCommand": "cd backend && python manage.py migrate && gunicorn your_project.wsgi:application"
    }
  }